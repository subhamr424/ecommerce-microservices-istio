# Leave empty when the frontend is served through the same Istio ingress gateway
# as the backend paths (/catalog, /search, /cart, etc — the default in this repo).
# Set explicitly only if the frontend is served from a different origin.
VITE_API_BASE_URL=
